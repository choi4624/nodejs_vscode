{
    "configurations": [
    {
        "name": "현재 파일 실행 (경로 반영)",
        "program": "${fileDirname}/${fileBasename}",
        "request": "launch",
        "skipFiles": [
            "<node_internals>/**"
        ],
        "type": "node",
        "cwd": "${fileDirname}"
    }
    ]

}